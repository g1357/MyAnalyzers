namespace MyAnalyzers.Analyzers
{
    /// <summary>
    /// Base class reserved for shared analyzer infrastructure (e.g., common diagnostic creation helpers).
    /// Concrete analyzers do not inherit from this class directly; extend as shared logic grows.
    /// </summary>
    internal abstract class NamingAnalyzerBase
    {
    }
}
